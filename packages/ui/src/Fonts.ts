import { Inter, Nunito } from "next/font/google";

export const inter = Inter({ subsets: ["latin"], display: "swap", weight: ["800", "600"] });
export const nunito = Nunito({ subsets: ["latin"], display: "swap" });
